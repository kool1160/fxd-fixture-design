name: RETIRED — M33.1 paid Codex dispatcher

# RETIRED BY REVIEW-CONTROL FOR COST SAFETY.
#
# This stale dispatch branch is retained only as historical evidence. It must
# never invoke Codex, consume provider API credentials, push implementation
# code, create pull requests, or advance FXD.
#
# Normal FXD implementation uses ChatGPT Codex Remote under the user's ChatGPT
# agentic allowance. Product-runtime API requests require separate explicit
# Review-Control authorization and are never implied by CONTINUE.

on:
  workflow_dispatch:

jobs:
  retired:
    name: Paid dispatcher is retired
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Fail closed
        shell: bash
        run: |
          echo "RETIRED: stale FXD paid GitHub Codex dispatcher is disabled."
          echo "Use ChatGPT Codex Remote; API spend requires separate explicit product-runtime authorization."
          exit 1
