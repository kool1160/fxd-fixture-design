{
  "name": "Kernel acceptance parser proof",
  "on": {
    "workflow_dispatch": {},
    "push": {
      "branches": ["main"],
      "paths": [".github/workflows/kernel-acceptance.yml"]
    }
  },
  "permissions": {
    "contents": "read"
  },
  "jobs": {
    "parser-proof": {
      "name": "Workflow parser proof",
      "runs-on": "ubuntu-latest",
      "steps": [
        {
          "name": "Confirm runner starts",
          "run": "echo workflow-parser-ok"
        }
      ]
    }
  }
}
